# FleetTools npmjs.com configuration
# Use public npm registry
registry=https://registry.npmjs.org/

# Authentication for npmjs.com (set NPM_TOKEN environment variable)
# For local development:
# export NPM_TOKEN=your_npm_access_token

# For GitHub Actions: NPM_TOKEN should be set as repository secret