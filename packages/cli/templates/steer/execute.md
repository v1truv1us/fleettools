# Fleet Steer Execute Prompt — {{slug}}

The change is in the execute phase. Run:

```bash
fleet steer task list {{slug}}
fleet steer task start {{slug}}
```

Then implement only the emitted task prompt. Preserve unrelated worktree changes.
