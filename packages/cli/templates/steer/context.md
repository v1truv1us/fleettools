# Fleet Steer Context Prompt — {{slug}}

You are preparing context for the change `{{slug}}`.

Write `specs/{{slug}}/00-context.md` with concise, evidence-backed notes:
- Current code paths and integration points
- Existing patterns to reuse
- Constraints, risks, and open questions
- Commands likely needed for verification

Do not implement code yet. Produce only the completed context artifact content.
