# Fleet Steer Plan Prompt — {{slug}}

Use the existing spec content below to write `specs/{{slug}}/03-plan.md`.

## Spec

{{prior_artifact}}

Create an implementation plan with small, independently verifiable steps. Include file paths, sequencing, risks, and test commands.

Do not implement code yet. Produce only the completed plan artifact content.
