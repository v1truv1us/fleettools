# FleetTools Documentation Cleanup & Version Fix

## Problem
1. Excessive redundant documentation (~20 files, ~5,000 lines of historical/redundant docs)
2. `fleet --version` returns `0.1.0` due to stale bun global binary with hardcoded version

## Root Cause (Version)
- Stale bun global binary at `~/.bun/bin/fleet` points to old `dist/index.js` with hardcoded `.version("0.1.0")`
- Homebrew binary at `/opt/homebrew/bin/fleet` returns correct `0.2.5`
- Current source code reads version dynamically from `package.json`

## Plan

### Phase 1: Fix Version Issue
- Remove stale bun global binary: `rm ~/.bun/bin/fleet`
- Verify: `fleet --version` returns `0.2.5`

### Phase 2: Delete Redundant Root Files (11 files)
Delete historical implementation artifacts and duplicates:
- `IMPLEMENTATION_PLAN.md`, `IMPLEMENTATION_SUMMARY.md`, `IMPLEMENTATION_PROGRESS.md`
- `IMPLEMENTATION_COMPLETE.md`, `PHASE2_IMPLEMENTATION.md`, `PUBLISHING_IMPLEMENTATION.md`
- `SETUP-SUMMARY.md`, `RALPH_FAILURE_ANALYSIS.md`, `SILENT_FAILURE_PREVENTION.md`
- `NPM_PUBLISHING.md` (overlaps with docs/publishing.md)
- `CLAUDE.md` (duplicates AGENTS.md)

### Phase 3: Delete Redundant docs/ Files
- `docs/IMPLEMENTATION_SPEC.md` (historical planning)
- `docs/research/` (entire directory - 5 historical research files)
- `docs/solo-orchestration-backlog.md` (orphaned)
- `docs/red-baron-returns.prompt.md` (orphaned)

### Phase 4: Delete Duplicate Directories
- `spec/` (duplicate of `specs/`)

### Phase 5: Consolidate Overlapping Docs
- Delete root `MIGRATION.md` (keep `docs/migration.md` - more comprehensive)
- Delete root `PUBLISHING.md` (keep `docs/publishing.md` - more comprehensive)

### Phase 6: Expand README.md
Current README is only 45 lines. Expand to include:
- What is FleetTools section
- Quick Start commands
- Documentation table linking to all docs/ files
- Updated project structure
- Keep existing Solo orchestration section

### Phase 7: Verify
- `bun run build` succeeds
- `fleet --version` returns `0.2.5`
- Clean root: only `AGENTS.md`, `CHANGELOG.md`, `DEPLOYMENT.md`, `README.md`, `sidebar.md`, `THIRD_PARTY_NOTICES.md` remain
- Clean `docs/`: only files referenced in `docs/README.md` remain

## Files to Modify
- Delete: 20+ files listed above
- Edit: `README.md` (expand content)

## Verification Commands
```bash
fleet --version          # Should return 0.2.5
bun run build            # Should succeed
ls *.md                  # Should show only 6 necessary files
ls docs/                 # Should match docs/README.md table
ls -d spec               # Should not exist
```
