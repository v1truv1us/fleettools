{
  "name": "@fleettools/server",
  "version": "0.2.1",
  "description": "FleetTools server components for VPS deployment",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm",
    "start": "bun dist/index.js",
    "dev": "bun src/index.ts",
    "test": "bun test-api.ts",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@fleettools/core": "workspace:*",
    "@fleettools/shared": "workspace:*",
    "@fleettools/squawk": "workspace:*",
    "@fleettools/events": "workspace:*",
    "@fleettools/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.9.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
