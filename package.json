{
  "name": "fleettools",
  "version": "0.1.0",
  "description": "FleetTools - AI Agent Coordination System",
  "private": true,
  "engines": {
    "bun": ">=1.0.0"
  },
  "workspaces": [
    "cli",
    "squawk",
    "server/api",
    "plugins/*"
  ],
  "scripts": {
    "install:all": "bun install",
    "build": "bun run build --filter=*",
    "test": "bun test",
    "lint": "bun run lint --filter=*",
    "doctor": "bun run cli/dist/index.js doctor"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
