{
  "name": "fleettools",
  "version": "0.1.0",
  "description": "FleetTools - AI Agent Coordination System",
  "private": false,
  "engines": {
    "bun": ">=1.0.0"
  },
  "workspaces": [
    "cli",
    "squawk",
    "server/api",
    "plugins/*",
    "packages/*"
  ],
  "scripts": {
    "install:all": "bun install",
    "build": "bun run build:workspaces",
    "build:all": "bun run build:workspaces",
    "build:workspaces": "bun run build:shared && cd packages/fleet-shared && bun run build && cd ../fleet-cli && bun run build && cd ../../cli && bun run build && cd ../squawk && bun run build && cd ../server/api && bun run build && cd ../plugins/claude-code && bun run build && cd ../opencode && bun run build && cd ../../packages/core && bun run build && cd ../db && bun run build && cd ../events && bun run build",
    "build:shared": "cd packages/fleet-shared && bun run build && cd ../fleet-cli && bun run build",
    "build:packages": "cd packages/fleet-shared && bun run build && cd ../fleet-cli && bun run build && cd ../core && bun run build && cd ../db && bun run build && cd ../events && bun run build",
    "docs": "cd docs && ls",
    "test": "bun test",
    "lint": "bun run lint --filter=*",
    "doctor": "bun run cli/dist/index.js doctor",
    "fleet:dev": "cd packages/fleet-cli && bun run dev",
    "fleet:build": "cd packages/fleet-cli && bun run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
